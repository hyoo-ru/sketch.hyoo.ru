$hyoo_sketch_element_switch $hyoo_sketch_element_base
	Element $mol_switch
		value? <=> switch_value? <= switch_value_default \one
		options *
			one \one
			two \two
	Options *
		^
		switch <= switch_options /
