namespace $.$$ {

	const { rem } = $mol_style_unit

	$mol_style_define( $hyoo_sketch_person_cursor, {

		position: 'absolute',
		pointerEvents: 'none',
		alignItems: 'flex-end',
		transition: 'all 0.5s ease-out 0s',

		Cursor: {
			height: rem(1.5),
			width: rem(1.5),
		},

	} )

}
